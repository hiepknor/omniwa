# Apps

Runtime apps compose packages into process roles. They must not own business logic.

Approved app roles:

- `api`
- `worker`
- `scheduler`
- `provider-runtime`
- `webhook-dispatcher`
- `projection-builder`
- `background`
- `metrics`
- `health`
