{
  "name": "@omniwa/app-metrics",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@omniwa/infrastructure-observability": "workspace:*",
    "@omniwa/observability": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  }
}
